import { VideoUpscaler } from '@/components/video-upscaler';

/**
 * Video Upscaler Main Route - /dashboard/video-upscaler
 * Upscale videos from 720p to 1080p or 4K using AI
 */
export default function VideoUpscalerPage() {
  return <VideoUpscaler />;
}
