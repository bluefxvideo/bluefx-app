/**
 * ReelEstate Photo Cleanup Route - /dashboard/reelestate/photo-cleanup
 * Content is rendered by the parent layout (reelestate/layout.tsx).
 */
export default function PhotoCleanupPage() {
  return null;
}
